(()=>{const original=window.fetch;window.fetch=async(input,init={})=>{const url=typeof input==='string'?input:input?.url||'';if(url.endsWith('/api/admin')&&init?.body){try{const body=JSON.parse(init.body);if(body?.action==='verify_payment'){const headers=new Headers(init.headers||{});const r=await original('/api/admin-payment',{...init,body:JSON.stringify({order_id:body.order_id,status:body.status}),headers});return r}}catch{}}return original(input,init)}})();